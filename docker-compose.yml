name: "hyperliquid"

services:
  node:
    platform: linux/x86_64
    restart: unless-stopped
    build: .
    ports:
      - "4000-4010:4000-4010"
    volumes:
      - ../files/hl-data:/home/hluser/hl/data
      - ../files/override_gossip_config.json:/home/hluser/override_gossip_config.json

  pruner:
    restart: unless-stopped
    build: ./pruner
    volumes:
      - ../files/hl-data:/home/hluser/hl/data

